const appName = 'Open Alliance for Cloud Adoption';
const styles = [
    'text-align: center',
    'display: block',
    'line-height: 40px',
    'background-color: #3E74AE',
    'color: #fff',
    'text-shadow: 1px 1px rgba(0, 0, 0, 0.65)',
    'box-shadow: 0 0 4px rgba(0, 0, 0, 0.4) inset',
    'padding: 8px 4px',
    'text-align: center'
].join(';');

console.info('%cOpen Alliance for Cloud Adoption', styles);
